[phases.setup]
nixPkgs = ["libwebp", "libpng", "libjpeg", "python3", "gcc", "libffi"]

[phases.build]
cmds = ["python3 -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"]

[phases.install]
cmds = []  # Leer lassen, da wir alles in build machen
